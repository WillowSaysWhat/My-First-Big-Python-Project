# Method: Brake

This method changes the speed variable by -5. It is executed when the `BRAKE` command is accepted.
```py
        def brake() {
            # check if broken

            # self.speed minus 5

            # check gear change
        }
```